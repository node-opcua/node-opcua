/**
 * Historizing variables prepared for the aggregate tests.
 *
 * Reached as `node-opcua-aggregates/testHelpers`. Internal to the monorepo: this tree exists
 * so the aggregate suite and the end-to-end suite build the same fixtures, not as supported API.
 */
export * from "./create_historizing_variables.js";
export * from "./helpers.js";
