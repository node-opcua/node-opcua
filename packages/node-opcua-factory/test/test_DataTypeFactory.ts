import "should";
import { DataTypeFactory } from "../dist/index.js";

describe("DataTypeFactory", () => {
    it("DataTypeFactory", () => {
        const _dataTypeFactory = new DataTypeFactory([]);
        //   dataTypeFactory.registerSimpleType("MyString", coerceNodeId("ns=1;i=1"));
    });
});
