// ----- this file has been automatically generated - do not edit
import { UAObject, UAMethod, UAProperty } from "node-opcua-address-space-base"
import { DataType } from "node-opcua-variant"
import { QualifiedName } from "node-opcua-data-model"
import { DTArgument } from "node-opcua-nodeset-ua/source/dt_argument"
import { UAFolder } from "node-opcua-nodeset-ua/source/ua_folder"
import { DTResult } from "./dt_result"
/**
 * The ResultManagementType provides mechanism to
 * access results generated by the underlying
 * system. Results can be managed in a local result
 * store of the Server. Methods and Objects with
 * Variables as well as Events and external file
 * stores can be used to provide the results to the
 * Client.
 *
 * |                |                                                  |
 * |----------------|--------------------------------------------------|
 * |namespace       |http://opcfoundation.org/UA/IJT/                  |
 * |nodeClass       |ObjectType                                        |
 * |typedDefinition |14:ResultManagementType ns=14;i=1017              |
 * |isAbstract      |false                                             |
 */
export interface UAResultManagement_Base {
    /**
     * defaultInstanceBrowseName
     * The default BrowseName for instances of the type.
     */
    defaultInstanceBrowseName: UAProperty<QualifiedName, DataType.QualifiedName>;
    getLatestResult?: UAMethod;
    getResultById?: UAMethod;
    getResultListFiltered?: UAMethod;
    releaseResultHandle?: UAMethod;
    /**
     * results
     * This is an instance of FolderType and is used to
     * organize multiple available results in the
     * underlying system which the server decides to
     * expose in the Address Space. It may contain no
     * results if the server does not expose results in
     * the Address Space at all or if no available
     * result matches the criteria for exposure in the
     * Address Space.
     */
    results?: UAFolder;
}
export interface UAResultManagement extends UAObject, UAResultManagement_Base {
}