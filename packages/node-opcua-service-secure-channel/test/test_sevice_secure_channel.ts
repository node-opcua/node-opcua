import * as service from "..";

describe("SecureChannel Service", () => {
    it("should instantiate a ChannelSecurityToken", () => {
        const _obj = new service.ChannelSecurityToken();
    });
});
